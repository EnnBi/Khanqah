// Flat version of the library route. Re-export the existing tab library
// screen so the home tile + floating bar can link here directly without
// duplicating logic.
export { default } from './(tabs)/library';
