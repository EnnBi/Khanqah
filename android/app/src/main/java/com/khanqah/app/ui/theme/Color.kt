package com.khanqah.app.ui.theme

import androidx.compose.ui.graphics.Color

// ── Dark — deep Islamic emerald + gold ──
val EmeraldBg1      = Color(0xFF0B2F27)  // primary background
val EmeraldBg2      = Color(0xFF0F3A30)  // secondary background / hero card
val EmeraldCard1    = Color(0xFF144236)  // surface / cards
val EmeraldCard2    = Color(0xFF1A4D3F)  // elevated card surface
val EmeraldIconBg   = Color(0xFF1E5A48)  // icon background tile
val EmeraldDivider  = Color(0xFF2A5A48)  // divider / stroke green
val EmeraldShadow   = Color(0xFF061A15)  // soft shadow tint

val GoldPrimary     = Color(0xFFC8A24A)  // primary gold — logo / highlights
val GoldRich        = Color(0xFFD4AF37)  // rich gold — active accent
val GoldAccent      = Color(0xFFE0C36A)  // light gold — hover / glow
val GoldMuted       = Color(0xFFB8933E)  // muted gold — disabled / soft UI

val TextPrimary     = Color(0xFFF5F3EE)  // off-white primary text
val TextSecondary   = Color(0xFFC9D1C5)  // muted green-white secondary text
val TextDisabled    = Color(0xFF8FA39A)  // disabled text

// ── Light — cream parchment + forest green + gold ──
val BackgroundLight  = Color(0xFFF0EDE5)
val SurfaceLight     = Color(0xFFFFFFFF)
val PrimaryLight     = Color(0xFF0F2E24)
val OnPrimaryLight   = Color(0xFFD4AF37)
val OnSurfaceLight   = Color(0xFF0F2E24)
val SecondaryLight   = Color(0xFF5A6B60)
val GoldLight        = Color(0xFFD4AF37)
val GoldSurfaceLight = Color(0xFFF5E9C8)
val BorderLight      = Color(0x1A0F2E24)

// ── Nav ──
val NavPillDark  = Color(0xFF1A4D3F)  // dark elevated green pill in dark mode
val NavPillLight = Color(0xFF0F2E24)  // forest pill in light mode
